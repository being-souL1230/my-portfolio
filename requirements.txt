# Core Flask dependencies (lightweight version)
Flask==3.1.1
Jinja2==3.1.5
Werkzeug==3.1.3
MarkupSafe==3.0.2
blinker==1.9.0
click==8.1.8

# Markdown rendering
Markdown==3.8

# Caching (lightweight in-memory cache)
cachelib==0.9.0

# Basic utilities
python-dateutil==2.9.0.post0
